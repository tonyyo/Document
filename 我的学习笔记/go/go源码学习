strconv.parserInt 源码学习：
参数：
1、s
2、base  # 转化为多少进制
3、bitsize  # 对应进制的最大长度，超过，则返回该长度对应的最大值

首先判断是无符号还是有符号， 有符号的计算，最终还是会转换成无符号的计算。
无符号计算的原理：
1、取字符串前两位，判断进制类型
2、遍历字符串s, 对应字符c：
  I、数字字符：d = c - '0'
  II、字母字符：d = lower(c) - 'a' + 10  # 因为前面有0-9
  III、c为下划线或者base == 0：error
3、n *= uint64(base)
   n1 := n + uint64(d)
   n = n1
